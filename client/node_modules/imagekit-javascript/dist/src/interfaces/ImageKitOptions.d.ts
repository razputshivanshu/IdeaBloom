import { TransformationPosition } from "imagekit-javascript/dist/src/interfaces";
export interface ImageKitOptions {
    urlEndpoint: string;
    sdkVersion?: string;
    publicKey?: string;
    transformationPosition?: TransformationPosition;
}
